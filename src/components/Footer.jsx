var React = require('react');
var Well = require('react-bootstrap').Well;

 var Footer = React.createClass({
   render: function() {
      return (
        <div>
          <Well bsSize="small">Carolina Belts</Well>
        </div>
      );
   }
 });

module.exports = Footer;
